# Build image
```
docker compose build
```

# Run
`docker compose up`

Then you can access
- pgadmin at localhost:5050
- FastAPI swagger at localhost:8000/docs


# Run tests
`docker compose run web pytest`
